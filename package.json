{
  "name": "tcrbac-net-doc-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Repository-local documentation and test reporting tools for TCRBAC.NET.",
  "scripts": {
    "allure:version": "allure --version"
  },
  "devDependencies": {
    "allure": "3.7.0"
  }
}
